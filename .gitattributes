# 统一换行：仓库内一律 LF，避免 Windows 上每次提交都刷 CRLF 警告。
# 二进制资源显式标记，防止 git 尝试做行尾转换。
* text=auto eol=lf

*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.avif binary
*.heic binary
*.ico binary
*.exe binary
*.dll binary
*.node binary
