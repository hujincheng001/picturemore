# 图压压 · 界面规范

> 本文件是 `prototype/index.html` 的文字化规范。原型即最终上线样式，任何实现都必须与原型逐项对齐。
> Token 采用三层结构（primitive → semantic → component），与 `design-system` 技能一致，可 1:1 映射到 Tailwind v4 的 `@theme`。

---

## 0. 设计读数

**Reading this as:** 桌面工具类产品界面（Operate 模式，非落地页）for 非技术普通个人用户，with a 极简排印 / 暖调单色 language，leaning toward 原生 CSS 三层 token + 系统 UI 字体 + 低强度反馈动效。

| 旋钮 | 值 | 理由 |
|---|---|---|
| `DESIGN_VARIANCE` | 4 | 工具界面必须可预期。用户不是来看设计的，是来把图压小的 |
| `MOTION_INTENSITY` | 3 | 只保留反馈动效（结果落位、按压回弹），无入场叙事、无无限循环 |
| `VISUAL_DENSITY` | 4 | 用户明确要求极简。信息靠对齐与留白组织，不靠边框与底色 |

**明确不做**：卡片、色块背景、图标系统、插画、渐变、玻璃拟态、装饰性状态点、滚动提示、字号超过 36px 的巨型标题。

---

## 1. Token

### 1.1 Primitive（原始值）

| Token | 值 | 用途 |
|---|---|---|
| `--bone-050` | `#FBFAF8` | 选中态的极浅底 |
| `--bone-200` | `#EFEDE8` | 窗口外的应用底色 |
| `--stone-100` | `#F4F2EE` | 次级底 |
| `--stone-200` | `#E9E6E0` | 悬停底、禁用底 |
| `--line-100` | `#E8E5DF` | 发丝分隔线 |
| `--line-200` | `#D8D4CC` | 控件描边、拖拽区虚线 |
| `--ink-900` | `#141414` | 正文、主操作底 |
| `--ink-700` | `#3A3835` | 次级正文 |
| `--ink-500` | `#6B6963` | 三级信息、说明文字 |
| `--amber-700` | `#8A5B00` | 唯一的有彩色。只用于"这样压会看出痕迹" |

**色域纪律**：全站只有暖灰一族，不混入冷灰。除 `--amber-700` 外不出现任何色相。不使用纯黑 `#000000`。

### 1.2 Semantic（语义别名）

| Token | 指向 | 用途 |
|---|---|---|
| `--bg-app` | `--bone-200` | 应用底色 |
| `--bg-surface` | `#FFFFFF` | 窗口、滑轨已填充段之外的底 |
| `--bg-subtle` | `--stone-100` | 次级底 |
| `--bg-hover` | `--stone-200` | 悬停底 |
| `--border` | `--line-100` | 所有分隔线 |
| `--border-strong` | `--line-200` | 所有控件描边 |
| `--fg` | `--ink-900` | 一级文字 |
| `--fg-2` | `--ink-700` | 二级文字（文件名） |
| `--fg-3` | `--ink-500` | 三级文字（说明、元信息） |
| `--accent` | `--ink-900` | 主操作。本产品的强调色就是黑 |
| `--caution` | `--amber-700` | 警示 |

### 1.3 Component（组件别名）

| Token | 值 |
|---|---|
| `--cta-bg` | `--accent` |
| `--cta-fg` | `#FFFFFF` |
| `--cta-bg-hover` | `#2E2C29` |
| `--cta-bg-disabled` | `--stone-200` |
| `--cta-fg-disabled` | `--fg-3` |
| `--track` | `--border` |
| `--fill` | `--accent`，超安全线时为 `--caution` |
| `--pct` | 滑块填充百分比（JS 写入） |

### 1.4 对比度实测（WCAG AA）

| 组合 | 比值 | 结论 |
|---|---|---|
| `--fg` / 白 | 17.9:1 | 通过 |
| `--fg-2` / 白 | 11.8:1 | 通过 |
| `--fg-3` / 白 | 5.6:1 | 通过 |
| `--fg-3` / `--bone-200` | 4.8:1 | 通过 |
| `--caution` / 白 | 5.8:1 | 通过 |
| 白 / `--cta-bg` | 17.9:1 | 通过 |
| `--cta-fg-disabled` / `--cta-bg-disabled` | 4.5:1 | 通过（禁用态另有豁免） |

---

## 2. 排版

### 2.1 字体族

```css
--sans: "Segoe UI Variable Text","Segoe UI","PingFang SC","Microsoft YaHei UI",system-ui,sans-serif;
--mono: "Cascadia Mono",Consolas,"SF Mono",ui-monospace,monospace;
```

只有两族。**不引入 Inter、Roboto、Open Sans；不引入任何衬线体。** 桌面工具必须看起来像系统的一部分，系统 UI 字是正确选择。若后续要自托管展示字体，只改 `--sans` 一处。

### 2.2 字号阶梯（四档，不再增加）

| Token | 值 | 用途 |
|---|---|---|
| `--fs-1` | `12px` | 说明文字、元信息、体积数字、按钮标签 |
| `--fs-2` | `15px` | 文件名、拖拽区提示、CTA |
| `--fs-3` | `20px` | 产品名 |
| `--fs-4` | `36px` | 滑块百分数（唯一的大字） |

字号是固定 rem/px，**不做流式缩放**。这是应用不是页面。

### 2.3 数字

所有体积、百分比、计数一律 `font-variant-numeric: tabular-nums`，体积数字用 `--mono`。理由：拖动滑块时数字宽度不能跳。

---

## 3. 间距、圆角、层次

- **间距基数 4px**：4 / 8 / 12 / 16 / 18 / 22 / 26 / 32。
- **圆角规则（全站唯一，无例外）**：
  - 窗口 `12px`（`--radius-window`）
  - 控件（按钮、拖拽区、格式选项）`6px`（`--radius-control`）
  - 内联小元素（焦点环、移除按钮）`4px`（`--radius-inline`）
- **层次只靠三样东西**：发丝线、留白、字号。**不使用卡片、不使用投影做层次。** 窗口本身有一层极淡的环境投影，是唯一例外（`0 20px 52px -26px rgba(20,20,20,.24)`）。
- **内容列宽** `max-width: 560px`，在窗口内水平居中。窗口 `min(880px, 100%)`。

---

## 4. 布局

**窗口**：`min(980px, 100%)` × `min(768px, calc(100vh - 96px))`。

**纵向三段**：

| 段 | 高度 | 说明 |
|---|---|---|
| 标题行 | 64px | **产品名在左上角**：「图压压」+ 小字「pictureMore」，左对齐，底部发丝线 |
| 主体 | `flex:1` | 左右双栏，中间一条发丝线 |
| （无底部栏） | | 操作区在左栏底部 |

**主体双栏**：`grid-template-columns: 328px 1fr`

**左栏（控制区，328px）**，纵向排列，段间距 26px：

| 顺序 | 内容 |
|---|---|
| 1 | 体积缩小：标签 + 36px 百分数同行，滑块，两端刻度 |
| 2 | 输出格式：四个文字按钮 |
| 3 | 存放位置：路径选择器 + 说明 |
| 4 | 质量承诺句 + 预估总量 |
| 5 | `margin-top:auto`：通栏 CTA + 本地处理声明 |

左栏可纵向滚动（内容超高时）。

**右栏（图片列表）**，纵向排列：

| 顺序 | 内容 |
|---|---|
| 1 | 拖拽区，52px，常驻 |
| 2 | 元信息行：左「共 N 张 · 总量」，右「清空列表」 |
| 3 | 文件清单，`flex:1` 内部滚动，每行 44px |

**空态**：主体退化为单栏（`grid-template-columns:1fr`），左栏整个隐藏，拖拽区吃掉整个右侧并变成纵向大区块。这样空态就是一个纯粹的邀请，不需要在不存在的控件上做禁用态。

**拖拽目标**是整个右栏（`.list-pane`），高亮反馈落在拖拽区上。这样用户把文件丢在列表任意位置都能命中。

---

## 5. 组件规格

### 5.1 拖拽区 `.drop`（右栏顶部）

| 状态 | 描边 | 底色 | 文字 |
|---|---|---|---|
| 默认（有图） | `1px dashed --border-strong` | 透明 | `--fg-3`，高 52px，横向 |
| 拖入悬停 | `1px solid --fg` | `--bone-050` | `--fg-2` |
| 空态 | 同上，`flex:1` 撑满右栏，纵向排列，字号升到 `--fs-3` | | `--fg-2` |

内容：`拖入更多图片，或` + `选择文件`（带下划线的文字按钮）+ 空态下的 `HEIC、JPG、PNG、WebP。一张也行，几十张也行`。
**拖拽与选择两条路径必须同时存在且同时可见。** 键盘用户走 Tab 到「选择文件」。

### 5.2 文件行 `.file`

| 状态 | 表现 |
|---|---|
| 默认 | 高 44px，底部 1px `--border`，末行无分隔线 |
| 悬停 / 内部聚焦 | 右侧「移除」由 `opacity:0` 变 1 |
| 处理中 `.busy` | 整行 `opacity: .3` |
| 已完成 | 体积单元格由 `4.2 MB` 变为 `4.2 MB → 1.5 MB`，新值淡入 320ms |
| 压不到目标 | 体积右侧**多一格**提示（琥珀）：`这张已经压到底了` / `质量已到下限，只压到 {x}%` |

移除按钮是**文字**不是图标。本产品不使用图标系统。

**关于第四格**：原型只画了 happy path，这一行原本只有三格。但「压不到目标」这个状态
原型从未覆盖，而 SPEC §9 明确要求行内说明，所以补了一格 —— 基准没画的地方不是
「不许动」，是「没规定」。实现上**只在有话说时才渲染**，没有提示的行仍是三格；
文件名那格是 `flex:1`，会吸收这点差异，体积与移除不会跳位。见 `docs/decisions.md` 的 T22-1。

### 5.3 滑块 `.slider`

- `<input type="range">`，`min=20 max=90 step=5`，默认 `65`。
- 轨道 2px，用 `linear-gradient(to right, var(--fill) 0 var(--pct), var(--track) var(--pct) 100%)` 自绘，JS 只写 `--pct` 与 `--fill`。
- 拇指 16px 圆形，白底 + 1.5px `--fill` 描边。
- 安全线：`71.43%` 处一条 1px × 9px 的 `--border-strong` 竖线（对应 70%）。
- **填充色即信号**：`≤70%` 为 `--fg`，`>70%` 转 `--caution`。不再额外加警告条。

| 状态 | 表现 |
|---|---|
| 默认 | 见上 |
| 悬停 | 拇指 `scale(1.1)` |
| 按下 | 拇指 `scale(.94)` |
| 键盘聚焦 | 拇指外 5px `rgba(20,20,20,.13)` 光环，输入框自身不画 outline |
| 超安全线 | 填充与百分数转 `--caution` |

### 5.4 输出格式 `.fmts`

四个文字按钮：`保持原格式`（默认）/ `JPG` / `PNG` / `WebP`。高 34px，`1px solid --border-strong`，圆角 6px。

| 状态 | 表现 |
|---|---|
| 默认 | 描边 `--border-strong`，文字 `--fg-3` |
| 悬停 | 描边 `--fg-3`，文字 `--fg-2` |
| 选中 | 描边 `--fg`，文字 `--fg`，底 `--bone-050` |
| 键盘聚焦 | 全局焦点环 |

**格式转换不是独立工具，它是"输出"的一个选项。** 不存在格式转换页签。

**键盘行为按 ARIA Authoring Practices 的 radiogroup 模式**（2026-09-22 补，见 T25-1）：

- 容器 `role="radiogroup"` + `aria-label`，每个按钮 `role="radio"` + `aria-checked`
- **整组只占一个 Tab 停靠点**：只有选中的那个 `tabIndex=0`，其余 `-1`
- 方向键（← → ↑ ↓）在选项间移动，**移动的同时选中**（radiogroup 的约定，不是先移动后回车）
- `Home` / `End` 跳第一个 / 最后一个

⚠️ 光写 `role="radio"` 是没有键盘行为的 —— 那只是给辅助技术的声明。
`role` 挂在 `<button>` 上不会带来任何原生行为，方向键必须自己接。

### 5.5 存放位置 `.picker`

一个 40px 高的通栏按钮，视觉上是一个可点击的字段：

```
┌────────────────────────────────────────┐
│ D:\照片\2026-09\processed        更改  │
└────────────────────────────────────────┘
```

| 状态 | 描边 | 底色 | 路径文字 | 「更改」 |
|---|---|---|---|---|
| 默认 | `--border-strong` | 透明 | `--fg-2`（等宽） | `--fg-3` + 1px 下划线 |
| 悬停 | `--fg-3` | `--bone-050` | `--fg-2` | `--fg` + `--fg` 下划线 |
| 键盘聚焦 | 全局焦点环 | | | |

- 路径为 `--mono` `--fs-1`，超长时右侧省略号截断，`title` 属性给完整路径。
- 下方固定一行说明：**「原图不会被改动，压缩后的新文件单独放在这里」**。这条必须常驻，它同时回答了"会不会覆盖原图"和"文件去哪了"两个疑问。
- **真实实现**：点击走 IPC → `dialog.showOpenDialog({ properties: ['openDirectory'] })`，返回绝对路径写回 `destPath`。原型里点击循环三个示例路径以演示状态变化。
- 导出路径**不在 CTA 下方重复出现**。同一个信息只出现一次。

### 5.6 主按钮 `.cta`

| 状态 | 底 | 文字 |
|---|---|---|
| 默认 | `--cta-bg` | `--cta-fg` |
| 悬停 | `--cta-bg-hover` | 白 |
| 按下 | 同悬停 + `scale(.99)` | 白 |
| 禁用 | `--cta-bg-disabled` | `--cta-fg-disabled` |
| 键盘聚焦 | 全局焦点环 | |

通栏，高 48px。文案随状态变化：`压缩这 N 张` → `处理中 i / N` → `再压一次`。标签必须单行，不得换行。

### 5.6.1 底部那行 `.foot`

CTA 下方那行**是四态的**（原型只在「常驻声明」与「完成提示」之间切换，
错误态与提示态是 2026-09-21/22 补的）：

| 状态 | 内容 | 颜色 |
|---|---|---|
| 有错误 | 批次错误文案（见 `SPEC.md` §8.4） | 琥珀 |
| 有提示 | `没有可压缩的图片`（一张都没加进来时） | 三级灰 |
| 跑完了 | `完成。原图没动，新文件在 {path}` | 三级灰 |
| 其余 | `图片只在这台电脑上处理，不会上传` | 三级灰 |

**优先级：错误 > 提示 > 完成 > 常驻声明。**
错误压过完成提示：一批被磁盘满中止时，先说「磁盘满了」比说「完成」有用得多。
提示压过完成提示：刚拖进来一个空文件夹，用户要的是「为什么没反应」，
而不是上一批的存放位置。

错误态用 `role="status"` 而不是 `role="alert"` —— 这是对用户主动触发的操作的反馈，
不该打断屏幕阅读器正在读的内容。

见 `docs/decisions.md` 的 T20-3 / T21-1 / T22-3。

### 5.7 焦点环（全局）

```css
:focus-visible{outline:2px solid #141414;outline-offset:2px;border-radius:4px}
```

滑块例外：用拇指光环替代整条轨道的 outline。**所有交互元素必须键盘可达且焦点可见。**

---

## 6. 动效

| 场景 | 实现 | 时长 |
|---|---|---|
| 窗口入场 | `opacity 0→1` + `translateY(6px)→0` | 520ms `cubic-bezier(.16,1,.3,1)` |
| 结果落位 | 新体积值 `opacity 0→1` | 320ms ease-out |
| 行处理中 | `opacity: .3` | 220ms |
| 拖拽区状态 | 描边色 + 底色 | 140ms |
| 按钮按压 | `scale(.99)` | 100ms |
| 滑块拇指 | `scale` | 160ms |

**只用 `transform` 与 `opacity`（颜色过渡除外）。禁止动画 `width` / `height` / `padding` / `margin`。**
全部动效包裹在 `@media (prefers-reduced-motion: reduce)` 里关掉。

---

## 7. 文案规则

- 全站简体中文，不预留多语言结构。
- 语气直白具体。禁用「赋能 / 一站式 / 极致 / 轻松搞定 / 无缝 / 释放」这类词。
- **零 em-dash**。中文里也不用「——」，改用句号或逗号断句。
- 不用 emoji，不用装饰性状态点，不用「01 / 02」式编号标签。
- 中黑点 `·` 每行最多一个。
- 质量承诺句是固定文案：**「不改尺寸，不裁剪，不重绘。只重新编码，正常观看看不出差别。」**

### 四态文案（优先级从上到下，命中即停）

| 优先级 | 触发条件 | 颜色 | 文案 |
|---|---|---|---|
| 1 | 输出格式 = PNG | 琥珀 | `PNG 是无损格式。照片类压不动，截图和纯色图能压很多。` |
| 2 | 输出格式 = JPG 且选中列表里有带透明通道的图 | 琥珀 | `选中的图里有 {n} 张带透明区域，转成 JPG 后透明部分会变成白色。` |
| 3 | 滑块 > 70% | 琥珀 | `超过 70% 后，放大到 100% 能看出压缩痕迹。图片尺寸仍然不变。` |
| 4 | 其余 | 三级灰 | `不改尺寸，不裁剪，不重绘。只重新编码，正常观看看不出差别。` |

优先级 2 的必要性：JPG 格式没有透明通道，透明区域必然被拍平成某个颜色（本产品用白色）。这是格式的硬限制而不是 bug，但用户不知情就会以为是压坏了。必须主动告知。

`{n}` 是当前选中列表里 `hasAlpha === true` 的图片数量，随列表变化实时重算。

---

## 8. 质量硬约束的界面表达

产品承诺「只改变文件大小，不能影响图片的正常展示」。界面必须把这个承诺**显性化**，不能只写进文档：

1. 常驻一行质量承诺句（见 §7）。
2. 滑块设 70% 安全线，越线时填充色与百分数同时转琥珀。
3. 越线后文案说明"图片尺寸仍然不变"，明确区分"压缩痕迹"与"尺寸变化"是两回事。
4. 选择 PNG 时主动劝阻，因为 PNG 无损格式本质上压不动。

实现层对应约束（见 `PRODUCT.md`）：绝不降分辨率、绝不裁剪、绝不重绘像素；**输出必须逐张断言宽高与原始一致，不一致即报错**。

---

## 9. 版本变更

| 项 | v2 | v3 | v4（当前） |
|---|---|---|---|
| 布局 | 单列居中 | 单列居中 | **左右双栏**：左控制 328px / 右列表 |
| 产品名位置 | 内容列顶部（随内容居中） | 同 v2 | **左上角**，全宽标题行左对齐 |
| 工具数 | 压缩 / 格式转换 两个页签 | 单一工具「压缩」，格式降级为输出选项 | 同 v3 |
| 拖拽区 | 只在空状态出现 | 常驻 52px 条 | 常驻，**位于右栏顶部**，拖拽目标为整个右栏 |
| 存放位置 | 静态文本，写在 CTA 下方 | 静态文本，写在 CTA 下方 | **可点击选择器**（40px 通栏按钮 + 更改），带"原图不会被改动"说明；CTA 下方不再重复显示 |
| Token | 一层扁平变量 | **三层结构**（primitive / semantic / component） | 同 v3 |
| 圆角 | 窗口 12 / 控件 5 | 窗口 12 / 控件 6 / 内联 4，成文规则 | 同 v3 |
| 字号 | 12 / 15 / 20 / 38 | 12 / 15 / 20 / 36 | 同 v3 |
| 图标 | 一个手绘 SVG 叉 | **全站零图标**，移除改为文字按钮 | 同 v3 |
| 强调色 | 墨黑 + 琥珀（滑块填充） | 不变，补全对比度实测 | 同 v3 |
| 动效 | 有 `transition: height`（检测报错） | 已移除，只动 transform / opacity / 颜色 | 同 v3 |
| PNG 劝阻 | 无 | 有 | 有 |

---

## 10. 交付检查

- [x] `detect.mjs --json prototype/index.html` → `[]`（零问题）
- [x] 全站零 em-dash
- [x] 全站零图标、零 emoji、零装饰性状态点
- [x] 全部文字对比度通过 WCAG AA
- [x] 每个交互元素具备 default / hover / focus-visible / active / disabled 全状态
- [x] 所有动效有 `prefers-reduced-motion` 兜底
- [x] 键盘可操作：Tab 顺序与视觉顺序一致，焦点环可见
- [x] 拖拽与选择两条加图路径同时可见
- [x] 存放位置可选择，且明确告知原图不会被改动
