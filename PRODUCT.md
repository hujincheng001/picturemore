# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Stack

Electron 44 + React 19 + Vite 8 + TypeScript 5.9 + Tailwind CSS 4 + sharp 0.35 + zustand 5 + electron-builder 26。
已由 `README.md` 与 `picturemore-dev` 技能锁定，本轮不重新选择。桌面壳（Electron）负责本地文件访问与图像处理，渲染层是标准 web 技术。

## Users

普通个人用户，非技术背景。使用发生在"图发不出去"的那一刻：微信 / 邮件 / 云盘提示体积超限，或办事系统要求"照片 ≤200KB、必须 JPG"。

- 使用频次低，单次数量从 1 张到几十张都有
- 不会阅读文档，不会调参数，界面必须自解释
- 对"图片被上传到服务器"有真实顾虑（证件照、合同、私密照片）

## Product Purpose

一个完全本地运行的图片处理工具箱，把"发不出去的图"变成"能发出去的图"。

成功的样子：用户拖入图片、选一个目标、拿到结果 —— 全程不需要理解任何图像参数，也不需要担心图片离开了本机。

## Positioning

**图片内容永不离开用户机器。** 压缩与格式转换全部在本机完成，断网可用、无遥测、无广告。

这是必须上传才能工作的在线工具（TinyPNG、iLoveIMG、Squoosh）无法真实复制的机制。工具类产品里，本地不是性能优化，是能力边界。

第二层定位：**只改文件大小，不改图片本身。** 大量在线压缩工具靠"悄悄降分辨率"来兑现压缩率，用户拿到的图在手机上看还行、打印或放大就废了。本产品承诺尺寸、构图、像素一律不动，只做编码层面的优化 —— 这是可以写进界面上的一句话承诺。

## Operating Context

- Windows 桌面应用，安装包约 120MB（Electron 运行时 ~80MB + sharp ~30MB + 前端产物 ~2MB）
- 核心功能在完全断网环境下必须可用
- 处理对象以手机拍摄的 HEIC / JPG 为主，也包含 PNG / WebP
- 导出默认落到 `原目录/processed`，绝不原地覆盖原图

## Capabilities and Constraints

v1 只做两个入口，共用同一条底层流水线（选图 → 队列 → sharp 处理 → 导出）：

1. **压缩**（做深）—— 一条"体积缩小百分比"滑块驱动，实时预估输出总量，逐张迭代逼近；不做质量数字、不做目标体积 chips
2. **格式转换**（做深）—— HEIC / PNG / WebP / JPG 互转，正确处理透明通道

**v1 明确不做：** 去 EXIF、改尺寸 / 裁剪、水印、拼接、批量重命名、调色 / 滤镜 / 锐化、AI 抠图（v2 走 `onnxruntime-node`）、文件夹监控、macOS / Linux 版本。

> 去 EXIF 与改尺寸已从 v1 移除（原方案的四入口收敛为两入口）。相关能力后续作为独立工具回归时，仍共用同一条流水线。

**质量硬约束（最高优先级，优先于任何压缩率指标）：**

> 压缩**只允许改变文件体积，不允许影响图片的正常展示**。
>
> 具体含义：绝不降分辨率、绝不裁剪、绝不重绘像素；只允许在编码参数上做感知无损的优化（质量档位保持在肉眼不可辨区间、色度抽样与元数据按需精简）。任何为了凑压缩率而牺牲观感的做法一律视为 bug，不是取舍。

**硬约束**（继承自 `README.md` 与 `picturemore-dev`，优先于一切实现选择）：

- 渲染进程不直接访问文件系统，全部走 IPC；`contextIsolation: true` + `nodeIntegration: false` 不可关闭
- 大图预览一律用缩略图（1000px 内），导出时才处理原图
- 绝不原地覆盖原图，一律输出到用户指定目录，文件名保持原样
- sharp 并发限制在 4–8，避免 libvips 线程池吃满内存
- 不引入任何需要联网的运行时依赖；前端资源全部打包，不用 CDN
- `src/main/image/` 必须是纯函数模块（TypeScript），不依赖 Electron API，可脱离 Electron 单测
- 压缩输出必须逐张校验尺寸与原始一致（宽高断言），校验失败即报错，不得静默交付

## Brand Commitments

- 产品名：**图压压**，英文副标 **pictureMore**
- 界面语言：仅简体中文，文案不预留多语言结构
- 语气：直白、具体、不夸张。不用"赋能 / 一站式 / 极致 / 轻松搞定"这类词

## Evidence on Hand

- **可复用资产**：`F:\AIGC\解疑助手\imagePro` —— 已能打出 NSIS 安装包的 Electron + React + Tailwind + sharp 图片压缩工具，其主进程 / 预加载 / sharp 用法 / electron-builder 打包配置可直接对照。
- 本机 4 个 Vite + React 19 + TS 项目的 `node_modules` 已装好，可复制 `package.json` 与配置文件。
- **尚无**真实用户数据、评测、截图或品牌视觉资产。未来工作不得编造用户量、客户名、评测成绩或性能基准。

## Product Principles

1. **一个控件就够。** 用户要的是"能发出去"，不是"质量 80 / 色度抽样 4:2:0"。能用一个百分数滑块表达，就不要暴露编码参数；能实时显示结果，就不要让用户点一下才知道。
2. **观感是底线，不是权衡项。** 压缩率再高，只要肉眼能看出差别就是失败。宁可压不动，也不交出糊图。
3. **本地是机制，不是营销词。** 任何功能都不得为了效果更好而联网；遇到取舍先满足这一条。
4. **单张即批量的特例。** 只维护一套列表模型，不做单张与批量两套界面。
5. **做不到就说做不到。** 压不到承诺的百分比时明确标注原因，绝不静默交付。
6. **工具应该消失在任务里。** 沿用桌面工具的可预期习惯，不为风格重新发明控件。

## Accessibility & Inclusion

键盘可操作：全部功能键盘可达、有可见焦点环、Tab 顺序与视觉顺序一致。
